# MySQL 数据源 profile
# 激活: SPRING_PROFILES_ACTIVE=mysql
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/liquidroute?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai&characterEncoding=utf8mb4
    username: root
    password: MRzjl52257700
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: none
  flyway:
    enabled: false  # MySQL 表由 datagrip-setup.sql 手动管理
